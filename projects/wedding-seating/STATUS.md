---
project: Aliança (Wedding Seating Planner)
status: LIVE — "Aliança", a deployed/branded/secure multi-tenant SaaS (Supabase Postgres+Auth+Storage + Vercel). Fases 0-F + many product features (couple layouts, dynamic moments w/ decoration+tasks+suppliers, per-moment layouts+seating+startTime, template photos, layout generic elements, venue calendar + per-wedding operational view + venue-only extra material, decor-catalog images). 309 tests. Prod https://wedding-seating-jade.vercel.app · repo github.com/tiagocesteiro/wedding-seating (main) — ALL pushed. Next: real pilot venue + optional hardening.
last_updated: 2026-07-21 (GitHub HEAD bdcddeb)
branch: PROD work on feat/production-supabase (vault) → synced to github.com/tiagocesteiro/wedding-seating (main). Frozen local SQLite site on feat/wedding-seating-engine.
tags: [wedding-seating, alianca, product, nextjs, supabase, vercel, live-saas]
---

# Wedding Seating Planner — Project Status

## ⭐ CURRENT PHASE: Professionalization (local prototype → real SaaS)
Feature build is essentially done (Plans 1-18, 239 tests, tsc+build clean, all owner feedback shipped). Tiago approved a plan to make it a **professional, deployable, multi-tenant product**. Full plan: `C:\Users\tiago\.claude\plans\soft-strolling-dolphin.md`.

**Architecture decision:** **Supabase (Postgres + Auth + Storage) + KEEP Prisma + tenancy enforced in the app layer (`src/lib/db/*.ts`)**; RLS added as defense-in-depth. NOT rebuilding the core in Lovable/v0 (would lose the seating engine, konva editor, geometry, PDF, 239 tests — the moat). Lovable/v0 only for landing page + UI primitives (Fase F).

**6 phases** (see plan file): 0 build-hygiene ✅ · A SQLite→Postgres+deploy (⏳ blocked on Tiago) · B images→Supabase Storage · C auth + accounts model (Profile, Venue.ownerId, Wedding.ownerId?, WeddingInvite) · D middleware + tenancy sweep across ~52 routes + RLS (the heavy lift) · E invites + cross-account sharing · F brand/design-system (parallel). ~10-16 solo days for 0-E.

**Security/RGPD:** app handles third-party PII (guest names, contacts, allergies=possible health data). Plan = EU region + app-layer tenancy + RLS + zod validation + privacy policy/DPA. **Recommended: external legal/RGPD review before charging; pentest before public scale.** (Pilot can launch without.)

**Fase 0 DONE** (commit a7ce184): `postinstall`/`vercel-build`/`engines` in package.json, `.nvmrc`, `.env.sample` (env contract), Prisma `binaryTargets` (+rhel for Vercel), `runtime="nodejs"`+`maxDuration=60` on heavy routes (generate/import/plan/image). No behavior change; local SQLite still works.

**Fase A DONE ✅ (2026-07-18)** — Prisma `provider=postgresql` + `directUrl`; SQLite migrations replaced by a single Postgres baseline (`20260717145703_init`) APPLIED to Supabase (project `tmpgncdkvhwlgcffigqn`, region **aws-0-eu-west-1 / Ireland = EU**); tests wired to an isolated **`test` schema** on the same Postgres (prod data untouched) — **239 tests green** (~97s vs 25s on SQLite, remote); `next build` clean; verified end-to-end live (create venue+wedding → 4 moments seeded in tx → read back → delete cascades). Vault commit `f57ba66` on branch `feat/production-supabase`.
**PRODUCTION REPO (new source of truth): `D:\wedding-seating`** → pushed to **https://github.com/tiagocesteiro/wedding-seating** (private, branch `main`, commit 537c605). Exported from the vault via `git archive` (tracked files only — **no `.env`**, no node_modules). The vault's `projects/wedding-seating/` stays as reference: branch `feat/wedding-seating-engine` = frozen local SQLite site (for testing), `feat/production-supabase` = the Postgres snapshot. NOTE: the standalone repo has **no `.env`** yet — Tiago must copy it there to run/migrate from `D:\wedding-seating`.
**Fase B DONE ✅ (2026-07-18)** — room images now upload to **Supabase Storage** (public bucket `floorplans`, created by `scripts/ensure-bucket.mjs`); `src/lib/upload.ts` writes via the service-role client (`src/lib/supabase/server.ts`, server-only) and stores the OBJECT PATH in `FloorPlan.image`; new single `src/lib/images.ts` `imageUrlFor()` (client-safe, uses only `NEXT_PUBLIC_*`) replaced the 5 duplicated helpers; the local-FS route `/api/uploads/[...path]` was DELETED. **241 tests green**, build clean, verified live (upload → renders from `storage/v1/object/public/floorplans/...`). Vault commit `ca35158`; pushed to GitHub as `b4d28bc`.

**Interim password gate DONE ✅** — `src/proxy.ts` (Next 16 renamed `middleware`→`proxy`) does HTTP Basic Auth when `SITE_PASSWORD` is set (401 on all pages+API otherwise); `SITE_PASSWORD` set in Vercel → production **verified private (401)**. Free alternative to Vercel's paid Deployment Protection. Removed by Fase D's real auth. Commits `8e417d7` (vault) / `a0991c4` (GitHub).

**Fase C DONE ✅ (2026-07-19) — auth + accounts, NO enforcement yet.** `@supabase/ssr` email+password auth: browser client (`src/lib/supabase/client.ts`), per-request server client, `updateSession` merged into `src/proxy.ts` (Basic gate FIRST, then Supabase session refresh — no auth redirects yet). Schema migration `add_auth_accounts` (additive): `Profile{id=auth uuid,email,role venue|couple|admin}`, `Venue.ownerId?`, `Wedding.ownerId?`, `WeddingInvite{token,role,expiresAt,...}` (invite endpoints are Fase E). Data-access `src/lib/db/profiles.ts` (getOrCreateProfile). Pages `/registar` (email+password + role choice quinta/casal) + `/login` + logout; `/admin` header shows email + Sair. **246 tests green**, build clean (no middleware-deprecation). Vault commit `ebb59b3`; GitHub `ac378a7`.
- ⚠️ **NEEDS TIAGO (Supabase config):** "Confirm email" is ENABLED on the project → login right after signup fails ("Email not confirmed"), and free-tier SMTP rate-limits repeat signups. For the pilot: Supabase Dashboard → **Authentication → Providers → Email → disable "Confirm email"** (or configure custom SMTP + Site URL). Until then the full signup→login flow can't complete (auth user + Profile ARE created).
- Follow-ups: role is client-chosen at signup (fine pre-enforcement); no app-level auth rate limiting (relies on Supabase).

**Fase D DONE ✅ (2026-07-20) — multi-tenant data isolation.** D1 (proxy auth-gates routes: unauth → redirect `/login` for pages, 401 for `/api`; ownership set on create; role-scoped `/admin` home + venue picker for couples; the interim `SITE_PASSWORD` Basic gate REMOVED — real auth replaces it) + D2a (authorization layer `src/lib/auth/access.ts` + `guard.ts`: `assert*Access`/`listXFor`; venue-side routes scoped) + D2b (wedding-side + leaf routes scoped). **Model:** venue role owns Venue+FloorPlan/TableType/Template; couple role owns Wedding+Guest/Group/Constraint/Moment/Table; couple READS the venue its wedding booked; venue has NO wedding access (sharing = Fase E). Every `/api/**` route guarded except `/api/auth/**` + `GET /api/venues/pickable` (intentional public venue directory). **Security review** (283b97f..8346cac) found + FIXED: **C1** (assignment PUT trusted body guest ids → cross-tenant write; now `saveAssignment(weddingId,…)` updateMany-scoped), **H1** (apply-template trusted body templateId → cross-venue read; now checks `template.venueId===wedding.venueId`), **L1** (template floorPlanId reparenting → same-venue check). Verified `admin` role can't be client-set (signup `VALID_ROLES=[venue,couple]`). **269 tests green** (incl. C1/H1 regression tests), build clean. Vault commit `9ec54b0`; GitHub `1e118c4`.
- **Accepted for pilot (not blocking):** H2 — a couple can set any `venueId` on its wedding (via the public picker) and thereby READ that venue's floorplans/templates (layouts/images, NOT guest PII). Fine while venue layouts aren't confidential; tighten in Fase E by gating the cross-read on a real venue-confirmed booking/invite. RLS DEFERRED (Prisma connects as DB owner → bypasses Postgres RLS, so app-layer `assert*` is the sole protection; RLS would only help a future direct-Supabase path).
- **NEEDS TIAGO:** (1) confirm "Confirm email" is OFF in Supabase so production signup→login works; (2) can REMOVE the now-unused `SITE_PASSWORD` env var from Vercel; (3) verify production: unauth→/login, register/login works, data isolated between two accounts.

**Fase E DONE ✅ (2026-07-20) — venue read-only PROGRESS oversight (model flipped).** Owner reshaped the model: the COUPLE creates + fills the WHOLE wedding (details, guests, arrangement picked from the venue's templates, seating); the VENUE no longer configures — it gets a **read-only progress view** of weddings booked at its venue(s). **STRICT RGPD: venue sees ONLY counts — never guest names/dietary/plan.** New `listVenueBookings(actor)` (`access.ts`) + `GET /api/venue/bookings` (venue role only, 403 for couple) returns per-wedding `{couple,date,guestEstimate, guests:{total,confirmed,pending,declined,seated}, arrangementPicked, seatingDone}` — counts only. Venue home (`admin/page.tsx` VenueSection) shows "Casamentos na tua quinta" progress cards + "o que falta" checklist, read-only (no links into the couple's private pages). `assertWeddingAccess` unchanged → venue still 403 on all wedding PII routes (verified). This also neutralises the H2 privacy concern: even if a couple points at any venue, the venue sees only PII-free counts. **273 tests green** (payload-has-no-PII assertion included), build clean. Vault commit `68f91e3`; GitHub `b9eb51e`.
- Minor product call flagged: `seated`/`seatingDone` counts any guest with `assignedTableId` incl. a declined guest the solver placed — faithful to data; decide later if "seated" should exclude declined.

**Fase F1 DONE ✅ (2026-07-20) — design-system + polish.** `src/components/ui/` primitives (Button/Card/Input/Select/Textarea/Field/Tabs/PageShell/Badge/Stat/AppHeader), Fraunces serif for the wordmark/titles/stats over the existing tokens. Polished: `/login`, `/registar` (role chooser), admin home (couple + venue — the venue "Casamentos na tua quinta" progress cards render as a real Stat/Badge/✓✗ dashboard), wedding+venue tab navs (shared Tabs + AppHeader), guest-table + plan toolbars. All user copy → **Portuguese** (Wedding→Casamento, Generate→Gerar plano, etc.). Responsive verified at ~375px. konva editor canvases/seating logic untouched. **273 tests green**, build clean. Vault commit `86f8825`; GitHub `b19fd8f`. Wordmark is a placeholder "Wedding Seating" — F2 swaps the real name/logo.

**Fase F2 DONE ✅ (2026-07-20) — brand + landing.** Product NAME = **Aliança** (owner-chosen; wedding ring + union). Shared `Wordmark` (Fraunces serif + interlocking-rings mark) across header/login/registar/landing; `src/app/icon.svg` favicon (interlocking rings, default favicon removed); `layout.tsx` metadata "Aliança — organiza o teu casamento", lang pt. New marketing **landing at `/`** (public; added to proxy allowlist): hero "O casamento inteiro, reunido num só lugar", Para casais / Para quintas cards, Três passos, Funcionalidades grid, final CTA + footer — bespoke editorial look, natural PT, responsive. **273 tests green**, build clean. Vault commit `fa8a06f`; GitHub `29912c0`.

**Brand accent = SAGE GREEN** (owner-picked over blue/terracotta/rose via a published comparison artifact). Applied via the single `--accent` token in globals.css + `icon.svg`; propagates app-wide. GitHub `fb0dee1`.

**ADMIN accounts DONE ✅ (2026-07-20).** Platform-admin (sees ALL venues + weddings) via an **`ADMIN_EMAILS`** env allowlist (comma-separated, case-insensitive) — the ONLY way to become admin; `getActor()` promotes those emails + upserts role="admin"; signup still rejects "admin" (no client self-promotion). New admin-only `GET /api/admin/overview` (403 for non-admin) + an **AdminSection** on `/admin` listing every venue + wedding with owner email + counts + open-links (admin bypasses tenancy, so it can open any wedding's full data). `.env.sample` documents `ADMIN_EMAILS`. **283 tests**, build clean. Vault `60c6f20`; GitHub `c169e6b`. **To activate: set `ADMIN_EMAILS` = your email in Vercel env (+ locally in `.env`), redeploy, log in.** RGPD note: admin sees all guest PII — operator privilege, use with care.

**COUPLE LAYOUTS DONE ✅ (2026-07-20) — couples create their own room layouts (4 waves, vault commits 8e75867→101ecee).** Owner asked: a couple can pick a template OR create their own layout (from a template copy, or a blank room by dimensions — "sem planta" as before), each layout saved distinct from venue templates; a couple can have SEVERAL layouts, each with ITS OWN seating; one is marked FINAL (drives venue progress + Visão geral + PDF). **Owner decisions:** (1) couple marks one layout final; (2) editor is SIMPLIFIED — start from a template/blank room, then add/remove/move tables (no photo upload/calibration/zones — that stays venue-side).
- **Model (additive migration `add_couple_layouts`, no drops):** `WeddingLayout{weddingId,name,isFinal,floorPlanId?(template bg)|width/depth/scale(blank),templateId?}` + `Table.weddingLayoutId?` + `LayoutSeat{weddingLayoutId,guestId,tableId @@unique[layout,guest]}` (per-layout seating — supersedes the single `Guest.assignedTableId`, which STAYS for the legacy flow / fallback). Data-access `src/lib/db/layouts.ts` (CRUD, exactly-one-final invariant, scoped per-layout tables+seating). `assertLayoutAccess` (couple owns via wedding; venue denied; admin bypass) + Table access branch.
- **Routes:** `GET/POST /api/weddings/[id]/layouts`, `PATCH/DELETE .../[layoutId]` (rename/setFinal/delete), `GET/PUT /api/layouts/[layoutId]/tables`, `GET .../plan`, `PUT .../assignment`, `POST .../generate` (runs the SAME seating engine on the layout's tables). create-from-template enforces `template.venueId===wedding.venueId` (H1).
- **UI:** the wedding "Seating plan" tab → **"Layouts"** index (`plan/page.tsx`: create from a venue template or a blank room, mark final, rename, delete) + per-layout editor (`plan/[layoutId]/page.tsx`) reusing PlanCanvas edit-mode (add/move/remove tables) + full seating (generate/drag/lock/swap/paint). `usePlan(weddingId, layoutId?)` was parameterized — same UI drives wedding OR layout endpoints (layout `/plan` returns `seats[]` + `background`, folded into assignedTableId locally).
- **Final-layout-aware:** CoupleView (Visão geral + PDF) + `listVenueBookings` + `getAdminOverview` now read the FINAL layout (its tables + LayoutSeat counts), with a safe FALLBACK to the legacy wedding arrangement when no layout is marked final. **293 tests green** (+ a tenancy test proving progress uses the final layout, not legacy assignedTableId), tsc + next build clean.
- **NOT retired (intentional, non-breaking coexistence):** the old Detalhes dinner-template apply-flow + `Guest.assignedTableId` still work; Visão geral/progress just PREFER the final layout. Future cleanup (optional): migrate legacy weddings onto layouts + retire the old plumbing.
- **PUSHED ✅ (GitHub `54de329`)** — synced vault→`D:\wedding-seating` via `git archive` + pushed to main; Vercel auto-deploys. Prod DB already had the migration (via `migrate dev`), so `migrate deploy` on Vercel skips it.

**MOMENTS → TABS + DECORATION + TASKS + SUPPLIERS ✅ (2026-07-20) — vault commits 9cd392b→6b7b1b1.** Owner request: move the wedding MOMENTS out of Detalhes into one TAB per moment (add/remove/rename); each moment gains a DECORATION section (venue catalog + couple's own items) and a TASKS section (replacing the notes box) with a responsible party (Noivos/Quinta/Fornecedor); Detalhes gains a FORNECEDORES section. Owner decisions: decoration = venue catalog + couple-custom; each moment keeps its arrangement (template) view + decor + tasks.
- **Model (additive migration `moments_decor_tasks_suppliers`, only DROPs the WeddingMoment (weddingId,kind) unique index):** WeddingMoment now DYNAMIC — `title?`/`order`, `kind` nullable (tag for the 4 seeded defaults; null for custom), id-based. New `Supplier` (wedding-owned), `MomentTask` (assignee couple|venue|supplier + supplierId? + dueDate? + done), `MomentDecor` (venue-catalog ref OR custom name/note + quantity), `DecorItem` (venue catalog: name/category/image?/price?). Data-access: moments.ts rewritten id-based (legacy kind-based helpers kept working via findFirst) + suppliers/tasks/momentDecor/decorCatalog. Access guards: assertMoment/Task/MomentDecor/Supplier/DecorItem.
- **Routes:** id-based moment ops at `/api/moments/[momentId]` (GET aggregate, PATCH rename/reorder/arrangement, DELETE) + collection `GET/POST /weddings/[id]/moments` (legacy `[kind]` route KEPT). tasks (`/moments/[id]/tasks`, `/tasks/[id]`), moment decor (`/moments/[id]/decor`, `/decor/[id]`), suppliers (`/weddings/[id]/suppliers`, `/suppliers/[id]`), venue catalog (`/venues/[id]/decor-items`, `/decor-items/[id]`). Arrangement/catalog-item/supplier refs are venue/wedding-matched.
- **UI:** Venue page → +DecorCatalog manager. Wedding nav (`layout.tsx`) → dynamic **one tab per moment** + "+ Momento". Moment page (`MomentDetail.tsx`) = Arrangement (template picker + thumbnail) + Decoration (catalog pick + custom) + Tasks (assignee Noivos/Quinta/Fornecedor + due date + done). Detalhes (`WeddingDetails.tsx`) — moment list/notes REMOVED (now tabs), **Fornecedores** section ADDED. **303 tests green** (10 new incl. tenancy for the new guards), tsc + next build clean.
- **Wave 5 DONE ✅** — Visão geral (`CoupleView`) rewritten to iterate the DYNAMIC moments (incl. custom ones), keyed by momentId; each moment section shows its arrangement (dinner→final layout; else template; else floor plan), a **Decoração** summary, and a **Tarefas** summary (X/Y concluídas). `getWeddingDetail` now includes ordered moments + tasks + decor. PDF export per moment kept. 303 tests green, tsc + build clean. Vault commit (see below).
- **PUSHED ✅ (GitHub `54de329`, together with layouts)** — Vercel auto-deploys; prod DB migration already applied via `migrate dev` so `migrate deploy` skips.

**LAYOUT ELEMENTS + CONSTRAINTS-ON-SEATING ✅ (2026-07-20).** Owner asks: (1) couple can add generic rectangular ELEMENTS (bar, pista de dança, ...) to their layouts; (2) guest CONSTRAINTS live on the seating/generate page. Done: `WeddingLayout.elements` (JSON, additive migration `layout_elements`); seeded from the template's floor plan on create; `saveLayoutElements` + `PUT /api/layouts/[id]/elements`; plan route returns the layout's own elements. PlanCanvas gained element editing (add/move/remove in editMode via `addElementMode`/`onMoveElement`/`onAddElementAt`/`onRemoveElement`); usePlan gained `elements`+add/move/remove; layout editor has "Adicionar elemento" (label + click to place). ConstraintsPanel widened to `{id,name}[]` and embedded (collapsible) in the layout seating editor; the standalone "Restrições" tab removed from the wedding nav. Dinner moment tab now shows the FINAL layout (read-only) instead of venue templates. 304 tests green; tsc + build clean.

**LAYOUTS MOVED INTO MOMENTS + hasSeating ✅ (2026-07-20).** Owner: remove the "Layouts" tab; the whole layout/seating process happens INSIDE each moment; a per-moment checkbox "tem plano de mesas (lugares marcados)?" (dinner yes, others no) — non-seating moments do layouts (tables+elements) with NO assigned seats. Owner chose MULTIPLE layouts per moment (mark one final). Done: `WeddingLayout.momentId` (weddingId kept for tenancy/guests) + `WeddingMoment.hasSeating` (additive migration `moment_layouts` with backfill: existing wedding layouts → dinner moment; dinner.hasSeating=true). layouts.ts CRUD is now momentId-scoped (list/create/final/getFinal per moment). Routes moved to `/api/moments/[momentId]/layouts(/[layoutId])`; old `/api/weddings/[id]/layouts` removed. Layout plan route returns `meta.hasSeating`; the editor (`plan/[layoutId]`) hides ALL seating UI (generate/paint/tray/constraints/tablelist) when the moment has no seating, keeping table+element editing. New `MomentLayouts` component + `MomentDetail` rewritten (Layout section w/ hasSeating checkbox + MomentLayouts, replacing the old template picker). "Layouts" tab removed from nav; old `/plan` index page deleted. CoupleView rewritten to render each moment's FINAL layout (seated only if hasSeating). Venue/admin progress counts the dinner (hasSeating) final layout. 304 tests green; tsc + build clean.

**VENUE CALENDAR + PER-WEDDING OPERATIONAL VIEW + EXTRA MATERIAL ✅ (2026-07-21).** Owner: venue wants (1) a calendar of its weddings (month/week toggle); (2) per booked wedding — the overview (final layouts, guest count, pending tasks) + a MATERIAL summary (couple's chosen decoration + venue-added EXTRA material like "tomadas triplas p/ DJ"); the extra material is venue-added per moment, visible ONLY to the venue.
- **Model:** `MomentMaterial{momentId,name,quantity,note}` (venue-only; additive migration `moment_materials`). Data-access `materials.ts`.
- **Access (venue window extended, still PII-FREE):** `assertVenueBooking(actor,weddingId)` — a venue may read a wedding booked at ITS venue (`venue.ownerId===actor`); couples denied (own routes); admin all. `assertMomentVenueAccess` + `assertMaterialAccess`. `getVenueWeddingView(weddingId)` returns a PII-free aggregate: guest COUNTS (never names/dietary), per moment {final layout summary, decoration chosen, extra materials, pending tasks (assignee)}. Test proves the payload contains no guest name/dietary.
- **Routes:** `GET /api/venue/weddings/[id]` (venue window); `GET/POST /api/moments/[id]/materials` + `PATCH/DELETE /api/materials/[id]` (venue-booking gated — couples 403).
- **UI:** `VenueCalendar` (month/week, weddings as chips → venue wedding page) on the venue home; `/admin/venue-wedding/[id]` page (`VenueWeddingView`): overview stats + material summary (aggregated decoration + extra material) + per-moment (decor, pending tasks, extra-material editor). BookingCard links to it. 309 tests green; tsc + build clean.

**LAYOUT ELEMENT PROPERTIES DIALOG ✅ (2026-07-21).** Double-clicking a generic room element (bar, pista de dança…) in the layout editor opens a properties dialog (name + width/depth in metres; converted via the layout scale). `usePlan.updateElement` persists; `PlanCanvas.onEditElement` on dbl-click/tap in edit mode. (GitHub 258252f)

**TASK/DECOR REFINEMENTS ✅ (2026-07-21).** (1) The table list under the layout editor now shows a table's CUSTOM name (double-click rename), not just "Mesa N". (2) The task "Responsável" dropdown lists the wedding's SUPPLIERS directly (Noivos / Quinta / <supplier by name>) instead of a generic "Fornecedor" + second select (value encoded "s:<id>"). (3) Tasks gained a free-text NOTE field (`MomentTask.note`, additive migration) — add-form field + shown under the task. (GitHub df6edbd)

**DECOR CATALOG IMAGES ✅ (2026-07-21).** Venue decoration catalog items can have photos. **Bulk import**: `POST /api/venues/[id]/decor-items/import` (multipart, multiple files) — each image becomes one catalog item named after its filename (extension stripped). Per-item set/replace: `POST /api/decor-items/[id]/image`. `DecorCatalog` UI: "Importar imagens" multi-select + a Foto column with thumbnails + per-row add/replace. Supabase Storage (`decor-<id>/…`), venue-guarded. `DecorItem.image` already existed. (GitHub bdcddeb). Couple-side decor PICKER still name-only (dropdown) — could show thumbnails later.

**Note — template example photos** live at: venue → **Templates** tab → each template's "Fotos de exemplo" (+ Adicionar foto). Couples see them in the moment's "A partir de um template" picker.

**REPOSITIONING → coordination platform (venue-owned) — FASE 1 DONE ✅ (2026-07-22, GitHub 9134635).** Owner reframed the product: from couple-centric seating planner to a **wedding coordination platform**, venue = primary owner, couple + suppliers = invited participants, single source of truth. Full plan in `C:\Users\tiago\.claude\plans\soft-strolling-dolphin.md` (top section). Owner decisions: supplier ACCOUNTS (role "supplier"), venue-first onboarding (venue creates + invites; existing weddings migrate), dietary→catering = aggregate-by-table (Fase 4).
- **Fase 1 shipped (participants foundation):** new `WeddingParticipant` (venue/couple/supplier per wedding) + `getWeddingRole`/`assertWeddingRole` in `access.ts` (unified role resolver, legacy owner fallback) — layered in additively; existing `assert*Access` untouched. Extended `Supplier` (invite slot: email/profileId/inviteToken/invited/acceptedAt), `WeddingInvite` (supplierId/service), `Profile.role` + signup accept "supplier". Additive migration `wedding_participants` **with backfill** (existing weddings → venue+couple participants). `createWedding` seeds participants. Invites: `invites.ts` (createSupplier/CoupleInvite, getInvitePreview, acceptInvite) + `POST /api/weddings/[id]/invites` (venue issues) + `GET/POST /api/invites/[token]` (preview/accept). UI: registar +Fornecedor; venue `WeddingParticipantsCard` (add supplier slot, generate/copy invite link, status); `/convite/[token]` accept landing (proxy-allowlisted, ?next= login flow); supplier portal (admin `SupplierSection` "os meus casamentos" + `/admin/supplier-wedding/[id]` scoped PII-free view = timeline + own tasks). Suppliers routes widened to venue+couple+admin. 320 tests green.
- **Fase 2 shipped (responsibility matrix) ✅ (2026-07-22):** new `WeddingService {kind, name?, providerType (venue|supplier|couple), supplierId?, status (planned|confirmed|done), note?}` — declares each service a wedding needs and WHO provides it (quinta in-house / external supplier slot / noivos). This is the generalization lever (decor in-house at one venue, external at another). Additive migration `wedding_services` (no backfill — empty until venue declares). `services.ts` data-access; `assertServiceAccess` in access.ts (venue/admin **write**, any participant **read** — matrix is PII-free). Routes `GET/POST /api/weddings/[id]/services` + `PATCH/DELETE /api/services/[serviceId]`. Venue UI `WeddingServicesCard` on the per-wedding view (add service, assign provider + status inline; external services point at a supplier slot from the participants card). 323 tests green.
- **Fase 3 shipped (interactions ledger / SSOT) ✅ (2026-07-22):** new `WeddingRequirement {momentId?, serviceId?, fromRole, fromProfileId?, toRole?, toSupplierId?, title, detail?, status (open|agreed|done), updatedAt}` + `WeddingRequirementComment` (lightweight reply thread) — the single source of truth for structured needs/agreements between parties (catering→quinta "8 mesas + 12 m lineares"; quinta→DJ "zona + tomadas"). Additive migration `wedding_requirements` (no backfill). `requirements.ts` data-access with **scoped listing** (RequirementScope all|supplier). `assertRequirementAccess` in access.ts: venue/admin read+write all; couple read all + write when involved; **supplier sees ONLY their slice** (raised by them, targets their slot, or concerns a service they provide) — unrelated supplier gets 404. Routes: `GET/POST /api/weddings/[id]/requirements` (scoped list + raise), `PATCH/DELETE /api/requirements/[id]` (status/edit/delete), `POST /api/requirements/[id]/comments` (reply). Shared UI `RequirementsPanel` (create/reply/status open→agreed→done) rendered on BOTH the venue per-wedding view (inbox, picks target supplier/couple) and the supplier portal (raise to venue, see own slice). 326 tests green (scoped-visibility + capability-matrix + lifecycle tests).
- **Fase 4 shipped (dietary-by-table for catering) ✅ (2026-07-22):** derived, **read-only, NO migration**. `getDietaryByTable(weddingId)` in `dietary.ts` — for the FINAL layout of a seated moment (jantar), counts `Guest.dietary` per table via `LayoutSeat`, normalized (empty→"Sem restrição", case-insensitive grouping), recomputed live, **never any names**. `assertDietaryAccess` (venue/admin + the wedding's **catering** supplier only — non-catering suppliers + couple denied; dietary = special-category RGPD data so the channel is deliberately narrow). Route `GET /api/weddings/[id]/dietary`. `DietaryByTableCard` (overall badges + per-table table) rendered in the catering supplier's portal (only when `service==="catering"`) and the venue view. 329 tests green (aggregate correctness incl. case-folding + positional "Mesa N" + gate matrix). Closes the "catering vê os pratos por mesas estilo agregado" requirement.
- **Fases 1–4 = the repositioning core is LIVE.** End-to-end now possible: venue invites catering → matrix assigns "catering→external supplier" → catering logs in, raises requirement "8 mesas + 12 m lineares" → venue agrees in inbox → couple does seating + dietary → catering sees dietary-by-table aggregate in real time.
- **Fase 5A shipped (audit log / Atividade tab) ✅ (2026-07-23):** append-only `AuditEvent {actorProfileId, actorRole, actorLabel, action, entityType, entityId?, summary, supplierId?, changes(Json)}` — the immutable record of every change + interaction, the "nunca se perde informação" guarantee, and the seed for notifications. Migration `audit_events`. `audit.ts`: `recordEvent` (**single writer, NEVER throws into the request path** — try/catch swallow) + `diff()` (field-level before/after) + scoped `listAuditEvents` (venue/couple all; supplier = authored OR own slot). `assertAuditAccess` (any participant reads). Route `GET /api/weddings/[id]/activity` (read-only). Instrumented the cross-entity channels with recordEvent + diffs: **requirements** (created/status_changed/updated/deleted/comment_added), **services** (created/updated/status_changed/deleted), **invite accept** (participant.joined), **tasks** (completed/reopened). Shared PT labels in `src/lib/labels.ts` (routes + UI). UI `ActivityFeed` (timeline: role badge + actor + summary + `campo: X → Y` diffs) in the venue view, the supplier portal (scoped "Atividade (a minha)"), and a dedicated **"Atividade" tab** on the couple workspace (`/admin/wedding/[id]/activity`). Decisions locked by owner: semantic + diffs; visible to all three parties scoped. 332 tests green.
- **Fase 5B shipped (awareness / novidades) ✅ (2026-07-23):** `WeddingParticipant.lastSeenActivityAt` (migration `participant_last_seen`). `unseenCountsForProfile(profileId)` in audit.ts — per-wedding count of events created after the participant's lastSeen, **excluding their own actions**, scoped (supplier = own-slot events only). `markActivitySeen`/`getLastSeenActivity` in participants.ts. Routes: `GET /api/activity/unseen` (map for the badges), `POST /api/weddings/[id]/activity/seen`; the activity GET now also returns `lastSeenAt`. UI: `ActivityFeed` highlights new events (accent border + "novo" badge) and auto-marks seen on open; `UnseenPill` + `useUnseenCounts` render "N novidades" pills on all three "my weddings" lists (supplier/couple/venue-bookings). 333 tests green. **Email digest deferred** (needs an email provider — Resend/SMTP; the AuditEvent + unseen infra is the foundation when we add it).
- **Fase 5C shipped (cleanup / less free-text) ✅ (2026-07-23):** three contained wins. (1) **Canonical dietary vocabulary** — `DIETARY_OPTIONS` + `<datalist>` (`DietaryDatalist` component) wired into GuestTable + AddGuestForm inputs; guests' diets cluster on consistent terms (keeps the Fase 4 catering aggregate from fragmenting) while still allowing free-typed values. (2) **Supplier-slot dedupe** — `findDuplicateSupplier` (name case-insensitive + same service, per wedding); the suppliers POST returns **409** on a duplicate; WeddingParticipantsCard surfaces the message. (3) **Structured requirement fields** — additive `WeddingRequirement.data Json?` (migration `requirement_data`); RequirementsPanel has optional Nº mesas / Metros lineares / Hora inputs → stored structured (venue reads exact numbers, no parsing) and shown as chips ("8 mesas", "12 m lineares", "19:00"). 335 tests green.
- **Repositioning Fases 1–5C COMPLETE and live.** The platform is a venue-owned coordination tool: participants + supplier accounts + invites (1) · services responsibility matrix (2) · interactions ledger/SSOT (3) · dietary-by-table for catering (4) · audit log + Atividade tab (5A) · unseen "novidades" badges (5B) · canonical vocab + dedupe + structured requirements (5C).
- **Fase 6A shipped (requirement handshake) ✅ (2026-07-23):** "Acordado" is now the counterpart's confirmation, not a free field. `WeddingRequirement.agreedByProfileId/agreedByRole/agreedAt` (migration `requirement_handshake`) = immutable "who agreed, when" trail. `canConfirmRequirement(wr, actorUserId, req)` in access.ts — the raiser can NEVER self-agree; the confirmer is the addressee (the specific `toSupplierId`, or `toRole` venue/couple; unaddressed → venue; admin too but never the raiser). PATCH route enforces it (403 otherwise) and **auto-reopens** an agreed requirement when its title/detail is edited (agreement can't be silently altered) → logs `requirement.agreed` / `requirement.reopened`. GET returns per-actor `canAgree`. RequirementsPanel replaced the raw status dropdown with **Confirmar acordo** (only the counterpart sees it) / **Marcar feito** / **Reabrir**, and shows "Acordado por Quinta · data". 337 tests green.
- **Fase 6B shipped (audit coverage) ✅ (2026-07-23):** extended `recordEvent` instrumentation beyond the interaction channels to the structural/cross-entity mutations: **layout.final_set** (only on the isFinal flip), **moment.created/renamed/deleted**, **decor.added/removed** (catalog + custom), **material.added/removed**, **guests.imported** (aggregate count). No new migration/tests (all additive recordEvent calls, which never throw). The Atividade timeline now reflects nearly all meaningful changes. Still NOT individually logged: per-guest edits/RSVP/dietary (high-frequency → would flood the log; the deeper Prisma-extension safety net remains the future answer for total field-level capture). 337 tests green.
- **Fase 6C shipped (dúvidas + log de estados) ✅ (2026-07-23):** the interactions ledger now has a `WeddingRequirement.kind` (`request` = Pedido | `question` = Dúvida; migration `requirement_kind`). A dúvida has a lighter lifecycle (Por responder → Resolvida) — **no confirmation handshake** (canAgree is false for questions). Kind-aware everywhere: `requirementStatusLabel(kind, status)` in labels.ts; create route accepts `kind` + kind-aware audit summaries ("Colocou a dúvida"/"Criou o pedido", "Respondeu à dúvida", "Marcou a dúvida … como Resolvida"). RequirementsPanel: Tipo selector (Pedido/Dúvida), kind badge, structured fields hidden for dúvidas, kind-aware statuses + actions (dúvida → Marcar resolvida/Reabrir; pedido → Confirmar acordo/Marcar feito/Reabrir). ActivityFeed gained a **"Tudo / Só estados"** filter (state-change actions only: requirement status/agreed/reopened, service.status_changed, task.completed/reopened, layout.final_set) so you can focus on state changes of pedidos, dúvidas, etc. 338 tests green.
- **Fase 6D shipped (request templates) ✅ (2026-07-23):** venues define reusable request/instruction templates once and fire them at each wedding's suppliers with a click. New venue-owned `RequirementTemplate {kind, service?, title, detail?, data?, order}` (migration `requirement_templates`); `requirementTemplates.ts` CRUD; `assertRequirementTemplateAccess` (same rule as the decor catalog — venue writes). Routes: `GET/POST /api/venues/[id]/requirement-templates` + `PATCH/DELETE /api/requirement-templates/[id]` (manage); `GET /api/weddings/[id]/requirement-templates` (templates resolved against the wedding — each shows its target supplier by service, or "Noivos", or "(sem fornecedor)") + `POST` same path (apply: bulk-creates WeddingRequirements from selected templates, addressed to the matching supplier slot / couple, linking serviceId, fromRole=venue, logged as requirement.created "… (template)"). UI: `RequirementTemplatesCatalog` on the venue page (`/admin/venue/[id]`, section "Pedidos template"); `SendTemplatesCard` on the per-wedding venue view (checkbox-pick + "Enviar selecionados"), refreshing the ledger via a new `reloadKey` on RequirementsPanel. 340 tests green.
- **Fase 6E shipped (supplier templates) ✅ (2026-07-23):** the template library is now **owner-agnostic** — venues AND supplier accounts each have their own. Schema: `RequirementTemplate` gained `ownerRole` (venue|supplier), nullable `venueId`, `supplierProfileId`, and `targetRole` (venue|couple, for supplier templates); migration `supplier_templates`. db split into `createVenueTemplate` / `createSupplierTemplate` + `listSupplierTemplates`; `assertRequirementTemplateAccess` handles both owners (supplier templates are private to their account). Routes: `GET/POST /api/supplier/requirement-templates` (supplier's own library); the item PATCH/DELETE gate + `weddings/[id]/requirement-templates` GET/POST **generalized by actor role** — a supplier gets their own templates resolved (target Quinta/Noivos) and sending creates requirements fromRole=supplier addressed to venue/couple; venue path unchanged. UI: `SupplierTemplatesCatalog` on the supplier home ("Os meus pedidos template", target Quinta/Noivos) + `SendTemplatesCard` reused on the supplier's per-wedding portal (grouped per recipient, per-block send). 341 tests green.
- **Fase 6F shipped (decor from venue OR rental company) ✅ (2026-07-23):** the decoration catalog is now **owner-agnostic** — a decor supplier (external rental company) has its own catalog alongside the venue's. Schema: `DecorItem` gained `ownerRole` (venue|supplier), nullable `venueId`, `supplierProfileId`; migration `decor_owner_agnostic`. `decorCatalog.ts` split into `createDecorItem`/`createSupplierDecorItem` + `listSupplierDecorItems`; `assertDecorItemAccess` handles both owners (supplier items private to their account; image route reused). Routes: `GET/POST /api/supplier/decor-items` (rental company's catalog); `GET /api/weddings/[id]/decor-catalog` returns the venue's items PLUS the wedding's assigned decor supplier's, each tagged with `source` ("Quinta" / rental company name); moment-decor POST validation generalized (item may belong to the venue OR a decor supplier on the wedding, keeping the per-item stock guard). UI: `SupplierDecorCatalog` on the supplier home ("O meu catálogo de decoração", name/category/price/stock + per-item image upload); the couple's `MomentDetail` now fetches the wedding catalog and shows each item's source. 343 tests green. Other decor/materials ideas noted but not built: cost/budget rollup (prices exist), supplier-declared material needs.
- **Fase 6G shipped (pretty decor + material images in the overview) ✅ (2026-07-23):** the wedding overview now shows the decoration pieces chosen per moment as a **visual gallery** — `DecorGallery` renders images with `object-fit: contain` on a soft tile + drop-shadow, so cut-out (transparent-background) PNGs read as floating objects; ×qty badge; pieces without an image fall back to tags. Rendered in the couple's "Visão geral" (CoupleView) and the venue's per-moment cards (replacing the text list). Decor images flow through `getWeddingDetail` (decorItem.image) + `getVenueWeddingView` (decor.image). **Material extra now supports an example image:** `MomentMaterial.image` (migration `material_image`) + `POST /api/materials/[id]/image` (reuses saveUploadedImage, venue-gated); the venue's material list has a per-line "Imagem/Trocar imagem" upload + a thumbnail, and material images surface in the overview. 343 tests green. Note: cut-out quality depends on the uploaded asset (we display transparency well but don't remove backgrounds).
- **Deferred / future:** email digest of novidades (needs a provider — Resend/SMTP; built on AuditEvent + unseen infra). Prisma client extension capturing field-level diffs on ALL writes via AsyncLocalStorage actor (deeper audit safety net). Migrate legacy couple-owned flows off the old model (currently additive, both work).

## 🎉 PROFESSIONALIZATION COMPLETE (Fases 0-F). Aliança is a live, branded, secure, multi-tenant SaaS.
Prod: https://wedding-seating-jade.vercel.app (Supabase Postgres EU + Storage; real auth; per-account isolation; venue read-only progress oversight; landing + brand). GitHub: github.com/tiagocesteiro/wedding-seating (branch main). Vault dev branch: feat/production-supabase; the frozen local SQLite site stays on feat/wedding-seating-engine.
**Owner TODOs:** confirm Supabase "Confirm email" OFF (so signup→login works in prod); optionally remove the now-unused `SITE_PASSWORD` Vercel env; end-to-end verify in prod (register a venue + a couple, check isolation + the landing).
**Backlog / optional hardening (before scaling to paying customers):** RLS defense-in-depth (Prisma bypasses it, so app-layer is the sole net today); a real venue↔couple booking handshake (currently the couple self-sets venueId — venue then sees only PII-free progress, so low risk); external legal/RGPD review + security pentest; custom SMTP + email confirmation for real signups; the `seated` counts-declined nuance; get a real PILOT venue using it.

**(historical) earlier Fase notes** — real route protection in `proxy.ts` (replace/augment the Basic gate with Supabase session redirects) + tenancy `where ownerId` scoping across `src/lib/db/*.ts` + transitive checks on the bare-`[id]` leaf routes + RLS. THE heavy lift; makes the app safe to open to real customers (each venue/couple isolated). Then Fase E (invites), Fase F (brand/landing).

**(historical) Fase A/Vercel notes below** (Tiago: connect repo, Build Command `npm run vercel-build`, EU region, the 6 env vars) — **MUST enable Vercel Deployment Protection**: there is still NO auth, so guest PII would be public. Then **Fase B is urgent**: image uploads write to the local filesystem and WILL BREAK on Vercel's ephemeral FS.

**(historical) Fase A was BLOCKED — waiting on Tiago** (checklist given in chat): (1) Supabase project `tmpgncdkvhwlgcffigqn` (confirm EU region) → fill `projects/wedding-seating/.env` with DATABASE_URL (pooled :6543 ?pgbouncer=true), DIRECT_URL (:5432), NEXT_PUBLIC_SUPABASE_URL, ANON_KEY, SERVICE_ROLE_KEY, STORAGE_BUCKET=floorplans; (2) create a PRIVATE GitHub repo for wedding-seating (must leave the vault — vault has secrets); (3) connect Vercel (build=`npm run vercel-build`, EU region). Supabase CLI 2.75.0 installed + `SUPABASE_ACCESS_TOKEN` already in env. **Note:** Tiago pasted the service_role key in chat once → it should be ROTATED. **Note:** I can't edit `.env` (hook-blocked) — Tiago fills it; Prisma loads it. **Pending:** Tiago's explicit OK to operate on this new Supabase project → then update `.claude/rules/supabase-access.md` (Pura Automation read-only; wedding-seating read/write; others forbidden).

## What it is
A local-first web app to build wedding seating plans. B2B2C vision (couples pay, venues are partners), but currently a **local tool** (`next dev`, SQLite) built to validate. Differentiator vs casamentos.pt: real calibrated venue floor plans + an automatic table-assignment engine + rich guest attributes/colors.

## How to run
```
cd "d:/Claude - PA/projects/wedding-seating"
npm run dev            # http://localhost:3000/admin
npm run test           # full vitest suite (104 tests as of 2026-07-10)
npx prisma migrate dev # if schema changed
```
- Stack: Next.js (App Router, TS), Prisma pinned **6.19.3** over SQLite (dev.db), react-konva, exceljs, Vitest.
- Sample import file: `d:/Claude - PA/convidados-exemplo.xlsx` (15 guests, groups + attribute columns).
- **Windows gotcha:** a running `next dev` locks the Prisma query-engine DLL; stop it before `prisma generate`/migrate, then restart.

## Architecture (key locations)
- **Pure seating engine:** `src/lib/seating/` (types, constraints, score, place, refine, solve) — no I/O, fully tested. Hard constraints: capacity, `separate`, fixed tables/locked guests (fixed-occupant-aware). Preferences: `together` pairs + priority-weighted multi-group reward (`GROUP_WEIGHTS=[10,5,2]`). Deterministic (greedy + hill-climbing refine).
- **Mapping:** `src/lib/plan/buildSeatingInput.ts` (DB rows → engine `SeatingInput`), `validate.ts` (live violations reusing engine validators), `colors.ts` (attribute→color legend).
- **Floorplan geometry (pure):** `src/lib/floorplan/` (geometry, spacing, boundary point-in-polygon, chairs).
- **Data layer:** `src/lib/db/` (Prisma singleton + per-entity access). Test DB provisioned once via `vitest.globalSetup.ts` (throwaway `prisma/test.db`).
- **API:** `src/app/api/` route handlers (venues, floorplans, tables, table-types, weddings, guests, groups, constraints, import, generate, plan, assignment, uploads).
- **UI:** `src/app/admin/` pages + `src/components/{editor,plan,guests,venue}/`. Konva canvases loaded via `next/dynamic {ssr:false}`; table coords stored in **image-natural pixels**, rendered with a shared `geoms`/displayScale.

## Plans done (specs+plans in `docs/superpowers/`, SDD ledger in `.superpowers/sdd/progress.md`)
1. **Foundation + seating engine** — Next.js+Prisma/SQLite+Vitest; pure engine, headless, tested.
2. **Floor-plan editor** — venue → upload room photo → calibrate scale → place/move/edit tables → save → reload. Natural-pixel coords.
3. **Guest import + grouping** — Excel parser (exceljs, accent-safe), import service, drag-to-group board, constraints editor (together/separate).
4. **Seating generation + plan view** — map wedding+floorplan → engine → Generate → render on plan → manual drag between tables with live over-capacity/separation highlighting; persistence in `Guest.assignedTableId`. Closed the fixed-occupant `separate` gap.
5. **Usability & legibility** — `Guest.locked` (persistent lock respected by Generate); manual add guest; "Casal" shortcut; named warnings (group/guest names not IDs); legible tables + per-table list; easy swap (drag chip onto chip).
6. **Guest attributes + colors** — `Guest.ageGroup/gender/dietary` (Excel + manual); pure color model; "Pintar por" filter + legend + attributes in list.
7. **Prioritized multi-group** — `Guest.extraGroups` (JSON ordered); engine group score → priority-weighted reward (prefers primary, falls back to secondary); per-guest priority editor.
8. **Venue table catalog** — `TableType` (name/shape/min/max seats/dimensions/quantity) per venue; per-table dimensions/minCapacity; `FloorPlan.minSpacing`; add-from-catalog in editor; spacing + under-min warnings. (Fixed a mass-assignment PATCH bug.)
9. **Walls + chairs** — `FloorPlan.boundary` polygon drawn over the photo (visual guide + out-of-bounds warning, NOT a solver constraint); chairs rendered around each table, occupied chairs colored by the selected attribute.
10. **Wedding tabs + editable floor plans** — wedding workspace uses tabs (Convidados & Grupos | Restrições | Plano de mesas) instead of an arrow link (shared `layout.tsx` + constraints route); admin lists a venue's existing floor plans so they can be reopened/modified (not only created). Also: `.claude/settings.json` hooks hardened to `$CLAUDE_PROJECT_DIR` absolute paths (survive cwd changes).
11. **Venue tabs + layout templates** — venue admin uses tabs (Mesas disponíveis [catalog] | Layouts de salas | Templates); `LayoutTemplate` model. (NOTE: the composition-only version from this plan was superseded by Plan 13.)
12. **Table shapes + realistic dimensions** — catalog: round (diameter), oval (large+small diameter), rectangular (length×width); `TableType.shape` gains "oval"; tables render on both canvases **to real scale** (circle/ellipse/rect via `tableRenderSize`), chairs on the real outline; drag alignment holds (shape + drop-overlay from the same size).
13. **Layouts (zones) + template-owned tables** — venue re-architecture: the Layouts editor = image + scale + **multiple zones** only (no tables); tables now live on **templates** (positioned): `LayoutTemplate.floorPlanId` (the layout), `Table.templateId` (positioned tables), `FloorPlan.zones` (multi-polygon). The Templates tab is a table mini-editor placing catalog tables on the chosen layout's image+zones background (realistic shapes, spacing + out-of-any-zone warnings), saved to the template. **Table.floorPlanId is now nullable**; the wedding flow still reads floor-plan tables until Plan 14.
18. **Editor & flow refinements (batch) — DONE + post-plan tweaks.** All 3 waves shipped, plus later owner tweaks: rect tables **seat along their length** (cabeceiras only beyond the ends when enabled), template-editor **visual spacing boundaries + drag barrier + add-outside-limit** (`spacingGeom`) and **min clearance from zone walls** (`zoneClearance`), extra-element **resize**, Detalhes **thumbnails render the arrangement (tables only, no zones/chairs/text)** (`ArrangementThumbnail`), seating plan shows **only occupied seats distributed symmetrically** (occupancy chairs), **Visão geral "Pintar por" → coloured dots → coloured PDF**, tab renamed **"Convidados"**. 239 tests. Original waves below: Waves 1-2 done: tabs renamed (**Seating plan**, **Visão geral**); seating plan hides zones, adds "Pintar por: **Grupo**", table list shows only names; Visão geral hides dinner zones + shows **N convidados · M confirmados** + per-moment **notes**; template editor shows the **layout name**, drops the generic circular table, and can **delete** placed tables; guest table gets **sorting + filters** (group/RSVP/name). Schema adds: `WeddingMoment.notes`, `Table.name` (double-click to rename on both canvases; seating plan via `PATCH /api/tables/[id]`), `Table.heads` (**rectangular tables with/without cabeceiras** — chair layout only, round/oval unaffected). The **dinner arrangement is now chosen by the venue in Detalhes** (with a thumbnail; the Seating plan no longer has a template picker — it seats into the applied dinner tables). Wave 3 (all done): **blank room by dimensions** (`updateFloorPlanDimensions` + `roomFit` — a photo-less floor plan defined by comprimento×largura → a to-scale room rectangle); **extra room elements** (`FloorPlan.elements` JSON — labelled coloured rectangles like dance floor/bar drawn in the template editor, rendered behind tables everywhere, exempt from spacing/zone checks); **snap/alignment guides** (`snap.ts` — dragging a table in the template editor snaps its centre to other tables' centres with dashed guide lines); **per-room PDF export** (`jspdf` — an "Exportar PDF" button per moment in the Visão geral outputs the room plan image + a per-table guest-name list for the dinner). **Plan 18 COMPLETE.**

17. **Plus-one + dots-only plan + arrangement-only couple view** — (1) the guest table has a **"Plus one"** column: picking guest B for guest A pairs them symmetrically (`Guest.plusOneId` `@unique` self-relation; `setPlusOne` sets/clears BOTH sides, re-pairing frees old partners; `PUT /api/guests/[id]/plus-one`), shows "(cross reference)", and both drop out of every other guest's dropdown. (2) **Vista do casal** now shows only the table arrangement (no seated guests) for every moment, dinner included. (3) The **sitting plan** (Plano de mesas) shows only each guest's colored **dot** per table by default; a table's guest **names** appear only when you click that table (`selectedTableId` gates chip visibility; assign-by-drop/fix/lock/drag/swap unchanged).

16. **Workflow roles: couple vs venue** — clarified who does what. **Couple** does only: import/introduce guests, **RSVP tracking** (`Guest.rsvp` = pending/confirmed/declined, editable "Confirmação" column + summary counts in the guest table), dinner seating, and viewing plans. **Venue** owns table design. Removed the couple's table editing (no more "Editar mesas"/move/add/remove on the Plano de mesas tab — the couple still picks a template, Generates, and drags/locks/swaps guests, but tables are the venue's design). Moments now reference a **venue template** (arrangement with tables): `WeddingMoment.templateId`; the Detalhes moment picker selects a venue arrangement per moment (ceremony/cocktail/dance), and **Vista do casal** renders each moment's tables read-only (dinner shows seated guests; others show the arrangement). Moments PUT validates the template belongs to the wedding's venue. New `GET /api/templates/[id]/tables`. (Follow-up: PlanCanvas/usePlan table-edit code is now dead/unreachable — left in place; couple-side editing removed only from the page.)

15. **Wedding details + plans-per-moment + couple view** — a wedding is now a real record: creation captures venue + date; a **Detalhes** tab (first tab) edits couple, both partners' name/email/phone, date, venue, guest estimate, notes (`PATCH /api/weddings/[id]` whitelist; new single `GET /api/weddings/[id]`). New `WeddingMoment` model (4 moments: ceremony/cocktail/dinner/dance, each with an optional `floorPlanId`, seeded on wedding create in a tx). **Seating applies only to the dinner** (the existing wedding tables/template = the dinner); the other 3 moments just pick a venue floor plan (`PUT /api/weddings/[id]/moments/[kind]`, validates the plan belongs to the wedding's venue). New read-only **Vista do casal** tab renders each moment: dinner = the seated tables (`PlanCanvas readOnly` — new prop, all interaction gated, Plan 14 editing unchanged), the others = the room image + zones; placeholders when unset. Data-access: `weddings.ts` (createWedding/getWeddingDetail/updateWedding) + new `moments.ts`. Tabs: Detalhes | Convidados & Grupos | Restrições | Plano de mesas | Vista do casal. Also (same session, pre-Plan-15): editable **guest table view** (row=guest, cols=attributes, inline group/attr edits — new `updateGuestAttributes` + guests PATCH), a warm **light site theme** (globals.css tokens/typography/soft buttons/accent tabs), softer guest+zone **color palette**, back-to-home nav, and a **min-table-spacing** control in the layout editor.

14. **Wedding consumes a template (editable copy)** — the wedding plan tab picks a template → `applyTemplateToWedding` COPIES its tables into the wedding (`Table.weddingId`), sets `wedding.floorPlanId` (=template's layout) + `wedding.templateId`, nulls guest seats; plan/generate now operate on the wedding's own tables and render on the template's layout background (image+zones). The couple **edits the copy** (move/add-from-catalog/remove) via `PUT /api/weddings/[id]/tables` → `saveWeddingTables` (ID-DIFF: update-in-place preserves seating, create new, delete-missing + unassign only those guests) **without touching the venue template**. `saveWeddingTables` was rewritten from delete-all+recreate to id-diff (delete-all would mint new ids and orphan every guest seat — `Guest.assignedTableId` has no FK). New data-access: `src/lib/db/weddingTables.ts` (`listWeddingTables`/`saveWeddingTables`/`applyTemplateToWedding`). **The wedding loop is now closed end-to-end.**

## Current data model (Prisma) — key fields added over time
- Guest: `groupId` (primary group), `extraGroups` (JSON ordered extras), `assignedTableId`, `locked`, `ageGroup`, `gender`, `dietary`.
- Table: `capacity` (=max), `minCapacity?`, `width?`, `depth?`, `fixed`, `x/y` (natural px), `shape`.
- FloorPlan: `image`, `scale` (px/m), `width/depth`, `minSpacing?`, `boundary?` (JSON polygon).
- TableType: venue catalog (name, shape, minSeats, maxSeats, width, depth, quantity).
- Venue, Wedding, Group, Constraint (type together|separate).

## NOT done / deferred (top of the backlog)
- **PDF export** with color coding + legend + per-table list (the printable deliverable for couples/venues). NEXT logical step (was "Plan 10", now the main remaining feature).
- Layout templates are done as **composition-only** (types × quantities, auto-grid on apply); storing exact per-image positions per template is still not done (lower priority).
- **Follow-ups (in `.superpowers/sdd/progress.md`):** refine perf at ~200 guests (incremental delta-scoring; groupReward is O(G²)); `GET /api/weddings/[id]`; Constraint→Guest FK; endpoint wedding-scoping/validation hardening; a few UI edge cases (boundary draft dropped on calibrate; AddGuestForm dead catch; chairs don't thread width/depth).

## Key decisions
- Local-first (SQLite, no cloud/auth/Stripe) until validated; migrate to Postgres later (schema kept portable).
- Branch left UNMERGED on `feat/wedding-seating-engine` by owner's choice; not pushed to a remote.
- Multi-group semantics = primary + ordered fallback (owner-specified), implemented as a priority-weighted engine reward.
- Walls/spacing/min-occupancy are warnings, not solver constraints (solver places guests into existing tables; it doesn't move tables).
- Accents work correctly in import (an earlier "scrambled groups" report was a corrupted bash-generated sample file, not an app bug).

## Verification status
- 239 automated tests green; `tsc --noEmit` + `next build` clean.
- Template editor barriers (Plan 18 T11/T12): tables keep min-spacing from each other AND from **zone walls** (`spacingGeom` + `zoneClearance`), with visual boundaries + drag barrier + add-outside-limit. Rect tables seat along their length (`chairs.ts`). Detalhes moment thumbnails render the **arrangement** (tables, no zones) via `ArrangementThumbnail`.
- Admin can **delete** weddings and venues (cascade) via "Apagar" buttons on `/admin` (`DELETE /api/weddings/[id]` + `DELETE /api/venues/[id]`; deleting a wedding cascades guests/groups/constraints/moments/tables; deleting a venue cascades floor plans/table types/templates and nulls referencing weddings' venueId).
- Floor plans (layouts) have a **name** (`FloorPlan.name`, editable in the layouts list + editor) and can be **deleted** (`DELETE /api/floorplans/[id]` — cascades the plan's tables + any templates built on it). Layouts list shows the name (fallback "Planta N").
- The dev DB (`prisma/dev.db`) was wiped clean of all QA/test data on 2026-07-13 — starts empty. (Note: orphaned uploaded images may remain under `data/uploads/`.) Every plan's UI was driven end-to-end with Playwright (generate, drag, colors, locks, swap, catalog, spacing, boundary, chairs, zones, template-apply, wedding table move/add/remove + template-unchanged) with screenshots in `.superpowers/`.
